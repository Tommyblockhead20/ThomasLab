PORT=8787
ROOM_CAPACITY=10
RECONNECT_WINDOW_MS=25000
HEARTBEAT_INTERVAL_MS=15000
# Production injects this securely. Never commit an actual database URL.
DATABASE_URL=
# Optional legacy override; normally Neon SSL is encoded in DATABASE_URL itself.
DATABASE_SSL=
# Optional comma-separated browser origins. Leave blank for unrestricted local development.
ALLOWED_ORIGINS=
